# Pinned for reproducible builds. Bump deliberately, not implicitly.
# Runtime
fastapi==0.138.2
uvicorn==0.49.0
httpx==0.28.1
python-dotenv==1.2.2
pydantic==2.13.4
# Test-only (harmless in prod; kept here so one install covers CI + local)
pytest==9.1.1
pytest-asyncio==1.4.0
